#include <iostream>
using namespace std;

int deQui(int n){
    if(n<=1){
        return 1;
    }
        return deQui(n-1) + deQui(n-2);
}
int main()
{
    int n;
    cin >> n;
    std::cout<<deQui(n);

    return 0;
}
