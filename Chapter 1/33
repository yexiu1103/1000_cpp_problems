#include <iostream>
#include <cmath>
using namespace std;

double square_root(int n){
    double result;
    for (int i=1; i<=n; i++){
        result = sqrt(2);
    }
    return result;
}
int main()
{
    
    int n;
    cin >> n;

    std::cout<<square_root(n);

    return 0;
}
